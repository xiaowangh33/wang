# IMU测试结果

> 2026-07-10 更新：当前 HI91 断电重连后上电波特率实测为
> `115200`，可稳定解析 HI91 约 `139~140 Hz`。本文下方的 `921600`
> 是 2025 年历史配置记录，不再是当前部署默认值。

## 测试时间
2025年1月16日

## 测试结果

### ✅ 成功项目
1. **串口连接**: `/dev/ttyUSB0` 已成功打开
2. **波特率**: **921600** (不是115200！)
3. **数据接收**: IMU正在发送数据
4. **编译状态**: `imu_debug_tool` 已成功编译
5. **原始工具测试**: `sudo ./hihost -p /dev/ttyUSB0 -b 921600 read` 运行成功 ✓

### ⚠️ 注意事项
1. **权限**: 可能需要sudo权限运行
2. **波特率**: 必须使用921600，不是115200

## 测试命令

### 使用编译好的调试工具
```bash
cd /home/gu/r_m_k_i_v1.2/rl/utils
sudo ./imu_debug_tool /dev/ttyUSB0 115200
```

### 使用原始hihost工具（已验证成功）
```bash
cd /home/gu/r_m_k_i_v1.2/imu/products-master/examples/C
sudo ./build/hihost -p /dev/ttyUSB0 -b 921600 read
```

## 下一步建议

1. **运行原始工具**: 使用hihost工具查看IMU是否能正常解析数据
2. **检查IMU型号**: 确认IMU的具体型号和协议版本
3. **查看文档**: 参考IMU产品文档确认数据格式
4. **调整解析**: 根据实际数据格式调整hipnuc_dec.c的解析逻辑

## 数据统计
- 数据速率: 约8.9KB/秒
- 数据格式: 二进制（非文本）
- 同步码: 未找到标准的0x5A 0xA5

## 备注
IMU硬件连接正常，数据流正常，但数据解析可能需要根据实际IMU型号进行调整。
